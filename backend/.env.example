PORT=3001

# Absolute path to the folder of media files to index
MEDIA_DIR=/media

# Writable directory for the SQLite database and thumbnails
DATA_DIR=/data

# Scan interval in minutes (used by the in-process scheduler)
SCAN_INTERVAL=30

NODE_ENV=development
